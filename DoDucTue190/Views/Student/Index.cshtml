@model IEnumerable<DoDucTue190.Models.Student>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentAge)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentAge)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StudentName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StudentName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StudentName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
